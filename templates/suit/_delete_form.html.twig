<form method="post" action="{{ path('app_suit_delete', {'id': suit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suit.id) }}">
    <button class="btn">Delete</button>
</form>
