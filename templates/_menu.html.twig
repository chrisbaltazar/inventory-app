{% set route = app.request.get('_route') %}

<nav class="navbar fixed-top navbar-expand-md bg-body-tertiary bg-primary" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Son de México 🇲🇽</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link {{ route is same as 'app_home_index' ? 'active':'' }}" aria-current="page" href="{{ path('app_home_index') }}">
                        Inicio
                    </a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link {{ route starts with 'app_user' ? 'active':'' }}" aria-current="page" href="{{ path('app_user_index') }}">
                            Usuarios
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ route starts with 'app_event' ? 'active':'' }}" aria-current="page" href="{{ path('app_event_index') }}">
                            Eventos
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ route starts with 'app_item' ? 'active':'' }}" aria-current="page" href="{{ path('app_item_index') }}">
                            Vestuario
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle {{ route starts with 'app_loan' ? 'active':'' }}" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">
                            Asignación
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ path('app_loan_new') }}">Nueva</a></li>
                            <li>
                                <hr class="dropdown-divider">
                            </li>
                            <li><a class="dropdown-item" href="{{ path('app_loan_user') }}">Por persona</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_loan_item') }}">Por pieza</a></li>
                        </ul>
                    </li>
                {% endif %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button"
                       aria-expanded="false">
                        {{ app.user.name }}
                    </a>
                    <ul class="dropdown-menu">
{#                        <li><a class="dropdown-item" href="#">Action</a></li>#}
{#                        <li>#}
{#                            <hr class="dropdown-divider">#}
{#                        </li>#}
                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>